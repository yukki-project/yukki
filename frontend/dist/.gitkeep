# Placeholder so that //go:embed all:frontend/dist succeeds when the
# frontend has not been built yet (first-time `go build ./...` in
# static-checks CI). The real Vite bundle is produced by
# `wails build` and is gitignored except for this file.
